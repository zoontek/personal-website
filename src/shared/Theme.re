open Css;

let baseFontFamily = "Karmilla, -apple-system, BlinkMacSystemFont, \"Segoe UI\", Helvetica, Arial, sans-serif";
let monospaceFontFamily = "SFMono-Regular, Consolas, \"Liberation Mono\", Menlo, Courier, monospace";

let primaryColor = hex("d3e8e1");
let textColor = hex("626174");
let titleColor = hex("3c3c4e");
let whiteColor = hex("fcfcfc");

let baseFontSize = 18->px;
